# OtakuMap 项目开发约束

二次元工具箱微信小程序。本文件为项目级开发约束，所有改动必须遵守。

## 开发工作流约束

1. **文档先行** — 先阅读 docs/ 下相关文档，理解现有设计与约定
2. **方案确认** — 给出实现方案，等待用户确认后再动手
3. **代码修改** — 按确认的方案执行改动
4. **验证** — 微信开发者工具预览（纯算法模块用 Node 直接跑测试），确保无回归
5. **文档收尾** — 更新 docs/ 中受影响的文档，保持文档与代码同步

## AI Vibecoding 约束

- **禁止硬编码** — 任何形式的硬编码都不可接受。魔法数字、配色、文案、规则参数一律抽到配置层（如 `utils/mahjong/config/`、`theme.json`、`THEME`）
- **代码为人类阅读而写** — 命名、注释、结构要让人能理解，而非仅让机器运行
- **遵循现有约定** — 新代码的风格、命名、所用库要与周边代码一致，不擅自引入新库或新模式
- **不过度设计** — 只解决当前任务，不顺手加额外抽象、配置项或防御代码。修 bug 不顺带重构
- **改动前先读** — 引用某文件/函数前先实际读取确认其存在，不凭记忆或文档臆测（docs 可能与代码不同步）
- **失败两次即换路** — 同一方案失败两次就停下来分析根因，换思路而非反复打补丁；偏离原意的换路要先说明并确认
- **诚实报告** — 测试失败就如实说明并附输出，跳过的步骤要讲明，不夸大「已完成」

## 项目专属约束

- **依赖管理** — 前端默认 pnpm，python 默认 poetry；本项目 npm 包需在微信开发者工具「工具 → 构建 npm」，构建产物落到 `packageFeatures/miniprogram_npm/`
- **分包边界** — TDesign 组件**仅在分包（packageFeatures）使用**，主包（pages/）保持轻量避免超 2MB
- **改 npm 配置后** — 修改 `project.config.json` 的 `packNpmRelationList` 或 package.json 依赖后，必须在开发者工具重新「构建 npm」
- **暗黑模式** — 全局基于 TDesign CSS 变量 + theme.json，新增样式需同时适配亮/暗两种模式
- **算法模块可测** — `utils/mahjong/` 为纯 CommonJS 模块、不依赖小程序 API，改动应配套 Node 测试用例验证

## 沟通约束

- 用中文回答
- 以审视的目光看待需求，主动指出潜在问题，并给出思考框架之外的建议；离谱处直接点破
